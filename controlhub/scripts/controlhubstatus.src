#!%INSTALL_PREFIX%/bin/escript
%% -*- erlang -*-
%%! -sname tempshellstats -hidden

main(_Ignore) ->
  {ok, Hostname} = inet:gethostname(),
  NodeStr = "controlhub@" ++ Hostname,
  Result = rpc:call(list_to_atom(NodeStr), ch_stats, report_to_string, []),
  io:format("~n~s~n", [Result]).

